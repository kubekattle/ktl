version: v1
rules:
  - id: github_token
    severity: block
    applies_to: [build_arg_value, oci_content, log_line]
    regex: '\b(ghp|gho|ghu|ghs|ghr)_[A-Za-z0-9]{20,}\b'
    message: GitHub token-like string detected (strict)

