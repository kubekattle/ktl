apiVersion: v1
kind: Pod
metadata:
  name: init-missing-readiness
spec:
  initContainers:
    - name: init
      image: busybox
      command: ["sh", "-c", "true"]
      readinessProbe:
        exec:
          command: ["sh", "-c", "true"]
  containers:
    - name: app
      image: nginx:1.25
      # missing readinessProbe
