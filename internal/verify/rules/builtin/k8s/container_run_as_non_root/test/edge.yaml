apiVersion: v1
kind: Pod
metadata:
  name: pod-level-nonroot-not-enough
spec:
  securityContext:
    runAsNonRoot: true
  containers:
    - name: app
      image: nginx:1.25
      # container securityContext missing, should still trigger.
