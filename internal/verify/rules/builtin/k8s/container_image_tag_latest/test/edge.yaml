apiVersion: v1
kind: Pod
metadata:
  name: unpinned-images
  namespace: default
spec:
  initContainers:
    - name: init
      image: busybox
      command: ["sh", "-c", "echo hi"]
  containers:
    - name: app
      image: nginx
    - name: sidecar
      image: alpine:latest
      command: ["sh", "-c", "sleep 3600"]
