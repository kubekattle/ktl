# Complex hooks stack fixture

This stack fixture is used by the integration test `TestKtlStackComplexHooksE2E`.

The `stack.yaml` contains placeholder tokens (e.g. `__KTL_TEST_NAMESPACE__`) that
the test replaces at runtime after copying this directory into a temp root.
