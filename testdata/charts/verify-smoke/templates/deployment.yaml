apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "verify-smoke.fullname" . }}
  labels:
    app: {{ include "verify-smoke.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "verify-smoke.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "verify-smoke.name" . }}
    spec:
      containers:
        - name: app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
