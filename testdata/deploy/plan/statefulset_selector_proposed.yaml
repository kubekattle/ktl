apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: db
  namespace: ns
spec:
  selector:
    matchLabels:
      app: db2
  serviceName: db
  replicas: 1
  template:
    metadata:
      labels:
        app: db2
    spec:
      containers:
        - name: db
          image: example/db:v2
