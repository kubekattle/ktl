version: "3.9"
services:
  ingest:
    build:
      context: ./services/ingest
    environment:
      PIPELINE: demo
  proxy:
    build:
      context: ./services/proxy
    depends_on:
      - ingest
